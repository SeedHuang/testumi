import { ArrayTable } from '@dncomponents/components/ArrayTable/schemas';
import { Card } from '@dncomponents/components/Card/schemas';

export const ArrayCards = Card;
ArrayCards.Addition = ArrayTable.Addition;
